# Very basic route optimizer (stub), replace with Google Maps API for production
class RouteOptimizer:
    def optimize(self, locations):
        # Pseudocode: Calls Google Maps Directions API to optimize route
        # For now, returns locations in current order
        return locations
